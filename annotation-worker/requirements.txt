# ============================================================
# Tesla Vision Platform - Annotation Worker 依赖
# ============================================================

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# HTTP 请求
httpx>=0.25.0

# 图像处理
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# PyTorch 已由基础镜像提供，此处仅列出 Python 依赖
numpy>=1.24.0

# 工具
pydantic>=2.0.0
python-multipart>=0.0.6

# Chinese-CLIP 专用依赖
# 注意: transformers>=4.45 依赖 torch.distributed.tensor.DTensor，
# 与基础镜像 pytorch/pytorch:2.4.0 不兼容，需固定到 4.44.x
transformers==4.44.2
timm>=0.9.0
# 降级回退: 当 transformers.ChineseCLIPProcessor 不可用时使用 cn-clip
cn-clip>=1.0.1
